<?php

/**
 * @file
 * Install, schema and uninstall functions for the prim_form module.
 */

/**
 * Implementation of hook_uninstall().
 */
function prim_teacher_result_uninstall() {
}

/**
 * Implementation of hook_install().
 */
function prim_teacher_result_install() {
}

/**
 * Implementation of hook_schema().
 */
function prim_teacher_result_schema() {
  $schema['prim_form'] = array(
    'description' => 'Table storing matrix layout.',
    'fields' => array(
      'mid' => array(
        'description' => 'recordID',
        'type' => 'serial',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
      'name' => array(
        'description' => 'The user identifier of a teacher form',
        'type' => 'varchar',
        'length' => 128,
        'not null' => TRUE,
      ),
      'abbreviation' => array(
        'description' => 'Matrix row name',
        'type' => 'varchar',
        'length' => 5,
        'not null' => TRUE,
      ),
      'weight' => array(
        'description' => 'weight of Matrix items',
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
    ),
    'primary key' => array('aid'),
  );
  return $schema;
}

//addition of the matrix table
function prim_teacher_result_update_7101() {
  $schema['prim_form'] = array(
     // table definition array goes here
  );
  db_create_table('prim_form', $schema['prim_form']);
}